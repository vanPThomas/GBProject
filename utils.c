#include <gb/gb.h>
#include "utils.h"
#include <stdio.h>
#include <stdlib.h>

// uint8_t random_between(uint8_t min, uint8_t max)
// {
//     int value = rand() % (max - min + 1) + min;
//     return (uint8_t) value;
// }